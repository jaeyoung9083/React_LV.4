import styled from "styled-components";

const StyledDiv = styled.div`
  padding: 20px;
  background-color: #0da0b0;
`;

const StyledInput = styled.input`
  height: 30px;
  border: 0;
  border-radius: 5px;
`;

const FlexDiv = styled.div`
  display: flex;
`;

export { StyledDiv, StyledInput, FlexDiv };
